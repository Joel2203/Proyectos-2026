Tema

grid explicito & implicito

🎯 Idea clave (3-5 líneas)

¿Qué hace y cuándo usarlo?

es para que cuando se creen nuevos elementos en el grid ya tengan un tamaño predefinido, ya sea en fila o columnas

⚙️ Sintaxis básica

    grid-auto-rows: (aca ponemos el tamaño que querremos);
    grid-auto-flow: (aca determinamos si queremos que se creen filas o columnas);
    grid-auto-columns: (aca ponemos el tamaño que querremos);


🧪 Ejemplo rápido

❌ Error que cometí

✅ Solución